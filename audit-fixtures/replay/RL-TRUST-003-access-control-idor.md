# RL-TRUST-003 Replay

1. Open two different project detail pages.
2. Use the access review panel to call out resource handles and client scopes.
3. Show the disabled route loader example and the tenant-aware patch summary.

Regression check:

- The live route still serves mock fixture data only.
- No tenant database, document fetch, or privileged export path is introduced.
