# RL-TRUST-005 Replay

1. Start on `/sign-in`.
2. Explain that personas are local-only and do not create sessions.
3. Open `/api/session` and show the trust boundary notes.
4. Compare with the disabled legacy trust-by-email snippet.

Regression check:

- No token, cookie, or privileged session is created.
- The scenario remains educational without becoming a real auth bypass.
